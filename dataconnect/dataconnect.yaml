specVersion: "v1beta"
serviceId: "freecodecamp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "freecodecamp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
