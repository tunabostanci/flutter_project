specVersion: "v1beta"
serviceId: "desktop"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "desktop-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
